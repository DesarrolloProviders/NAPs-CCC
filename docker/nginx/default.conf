# Vacío a propósito: reemplaza al default.conf de la imagen oficial (vhost "localhost" con la página de bienvenida),
# que si no respondería a cualquier Host desconocido en :80. Montado en compose.yml sobre /etc/nginx/conf.d/default.conf.
